import React from 'react';
import PropTypes from 'prop-types';

const FeaturedEntrepreneurs = (props) => {
  let data = props.data;
  const styles = {
  }
  return (
    <div>
      Featured
    </div>
  )
}

FeaturedEntrepreneurs.propTypes = {
  data: PropTypes.array.isRequired
}

export default FeaturedEntrepreneurs;
